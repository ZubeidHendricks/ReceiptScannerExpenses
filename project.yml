# Generated by AppFactoryKit appgen. Run: xcodegen generate
name: ReceiptScannerExpenses
options:
  bundleIdPrefix: com.zubeid
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  AppFactoryKit:
    url: https://github.com/ZubeidHendricks/AppFactoryKit
    from: "0.1.0"

targets:
  ReceiptScannerExpenses:
    type: application
    platform: iOS
    sources:
      - Sources
    dependencies:
      - package: AppFactoryKit
        product: AppFactoryKit
      - package: AppFactoryKit
        product: AppFactoryKitRevenueCat
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: "ReceiptScannerExpenses"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        NSCameraUsageDescription: "ReceiptScannerExpenses uses the camera to capture and process your content."
        NSPhotoLibraryUsageDescription: "ReceiptScannerExpenses needs access to your photos to import and process images."
        NSPhotoLibraryAddUsageDescription: "ReceiptScannerExpenses saves your results to your photo library."
    settings:
      base:
        PRODUCT_NAME: ReceiptScannerExpenses
        PRODUCT_BUNDLE_IDENTIFIER: com.zubeid.receiptexpense
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"
        GENERATE_INFOPLIST_FILE: NO
        CODE_SIGN_STYLE: Automatic

  ReceiptScannerExpensesTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests
      - path: Sources/ReceiptService.swift
    settings:
      base:
        PRODUCT_NAME: ReceiptScannerExpensesTests
        PRODUCT_BUNDLE_IDENTIFIER: com.zubeid.receiptscannerexpenses.tests
        GENERATE_INFOPLIST_FILE: YES
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"

schemes:
  Tests:
    build:
      targets:
        ReceiptScannerExpensesTests: [test]
    test:
      targets:
        - ReceiptScannerExpensesTests
